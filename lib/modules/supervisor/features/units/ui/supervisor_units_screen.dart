import 'package:flutter/material.dart';

class SupervisorUnitsScreen extends StatelessWidget {
  const SupervisorUnitsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('SupervisorUnitsScreen'));
  }
}