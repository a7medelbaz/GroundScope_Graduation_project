import 'package:flutter/material.dart';

class SupervisorTasksScreen extends StatelessWidget {
  const SupervisorTasksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
