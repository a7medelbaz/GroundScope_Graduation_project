import 'package:flutter/material.dart';

class SupervisorAddReportScreen extends StatelessWidget {
  const SupervisorAddReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(child: Center(child: Text('AddR')));
  }
}
